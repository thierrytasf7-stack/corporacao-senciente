# Atualizar Client ID no mcp.json

## ✅ Client ID Correto Encontrado!

O Client ID correto é: `88zOFYpcPpIfRXkfsnNBSNMDmeMLz1KB`

## 🔧 Próximos Passos

### 1. Verificar Client Secret

Na página "Settings" que você está vendo:
- O campo "Secret" está mascarado (`************`)
- **Clique no ícone de refresh** (↻) ou **copy** (📋) para ver/copiar o Secret
- Confirme se o Secret no `env.local` está correto para este Client ID

### 2. Atualizar mcp.json

✅ Já atualizei o `mcp.json` com o Client ID correto!

### 3. Verificar Authorization URL

Volte para a página "Authorization" (no menu lateral) e:
- Copie a URL "Classic Confluence API authorization URL"
- Use essa URL para autorizar o app

### 4. Reiniciar Cursor

Depois de autorizar:
- Reinicie o Cursor completamente
- Teste: "Liste todos os espaços do Confluence"


























