You are an expert TypeScript developer. Create a production-quality TypeScript file with these STRICT requirements:

## File: math-utils.ts

## Functions (5 total, all exported as named exports):

### 1. clamp(value: number, min: number, max: number): number
- Clamp value within [min, max] range
- MUST throw RangeError if min > max
- Works with negative numbers and zero

### 2. lerp(start: number, end: number, t: number): number
- Linear interpolation: start + (end - start) * t
- t is NOT clamped (extrapolation allowed for t<0 or t>1)

### 3. roundTo(value: number, decimals: number): number
- Round to N decimal places
- MUST throw RangeError if decimals < 0 or not integer
- Use Number.EPSILON correction for floating-point edge cases like 1.005

### 4. fibonacci(n: number): number
- Return nth Fibonacci number (0-indexed: F(0)=0, F(1)=1, F(2)=1...)
- MUST throw RangeError if n < 0 or not integer
- Use iterative approach (not recursive)

### 5. isPrime(n: number): boolean
- Return true if n is prime
- Return false for: n<2, non-integers, negative numbers
- Use trial division with 6k±1 optimization

## Quality Requirements:
- Full JSDoc with @param, @returns, @throws, @example tags on every function
- TypeScript strict mode compatible
- Input validation with RangeError for invalid inputs
- Add commented test section using console.assert() with descriptive messages
- Minimum 5 assert tests per function (25+ total)
- Tests MUST verify: normal cases, edge cases, error throwing, boundary values
- NO console.log tests - use console.assert ONLY
- Export all functions as named exports