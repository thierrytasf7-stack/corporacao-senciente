Create a TypeScript file at tests/benchmark/aider-results/string-utils.ts with the following functions, all with full JSDoc documentation and TypeScript types:

1. capitalize(str: string): string - Capitalizes first letter of string
2. slugify(str: string): string - Converts string to URL-friendly slug (lowercase, hyphens instead of spaces, remove special chars)
3. truncate(str: string, maxLen: number, suffix?: string): string - Truncates string at maxLen, adds suffix (default '...')
4. countWords(str: string): number - Counts words in string
5. reverseWords(str: string): string - Reverses order of words in string

Export all as named exports. Add a commented test section at the bottom with at least 3 test cases per function.