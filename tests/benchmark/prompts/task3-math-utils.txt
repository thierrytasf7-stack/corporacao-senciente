Create a TypeScript file at tests/benchmark/aider-results/math-utils.ts with the following functions, all with full JSDoc documentation and TypeScript types:

1. clamp(value: number, min: number, max: number): number - Clamps value between min and max
2. lerp(start: number, end: number, t: number): number - Linear interpolation between start and end
3. roundTo(value: number, decimals: number): number - Rounds value to specified decimal places
4. fibonacci(n: number): number - Returns nth Fibonacci number (0-indexed)
5. isPrime(n: number): boolean - Returns true if n is prime

Export all as named exports. Add a commented test section at the bottom with at least 3 test cases per function.