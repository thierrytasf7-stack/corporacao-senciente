# Incomplete Task

This task is missing the required TASK-FORMAT-SPECIFICATION-V1 fields.

It only has a title and description, no YAML frontmatter.

## Usage

Just a placeholder task that will fail validation.
